# Frontend Development Environment Variables

# API Configuration
VITE_API_BASE_URL=http://localhost:3001/api
# Since we're using webpack proxy, the API calls will be proxied to http://localhost:3000

# Environment
NODE_ENV=development
